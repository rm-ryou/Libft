#include "libft.h"
#include <string.h>

int main(int argc, char **argv)
{
    int num1 = ft_atoi(argv[1]);
    int num2 = ft_atoi(argv[1]);

    printf("ft_atoi = %d\n", num1);
    printf("   atoi = %d\n", num2);
    return (0);
}